### cocone.py

**Purpose:** `cocone.py` reconstructs a 3D surface mesh from a point cloud using the Cocone algorithm (via 3D Delaunay in 4D lifting and cocone filtering).

**How to run:**

```bash
pip install -r requirements.txt

cd project2

python cocone.py
